---
phase: 03-infrastructure-hardening-and-database-migrations
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [.planning/ROADMAP.md, .planning/REQUIREMENTS.md]
autonomous: true
gap_closure: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06]

must_haves:
  truths:
    - "ROADMAP.md Phase 3 Success Criterion #2 does NOT list reasoning-engine 2GB — it only lists the 5 existing services (Neo4j 2GB, Qdrant 1GB, PostgreSQL 512MB, n8n 512MB, data-sidecar 512MB) and notes reasoning-engine mem_limit is deferred to Phase 8"
    - "ROADMAP.md Phase 3 Success Criterion #4 clarifies that the Phase 3 deliverable is the .env.example GEMINI_API_KEY entry — live API validation is a runtime/human verification item deferred to Phase 8 when the reasoning-engine service exists"
    - "REQUIREMENTS.md INFRA-03 clarifies that reasoning-engine mem_limit is set when the service is created in Phase 8, not in Phase 3"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 3 Success Criteria #2 and #4"
      contains: "data-sidecar 512MB"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Clarified INFRA-03 scope"
      contains: "reasoning-engine 2GB deferred to Phase 8"
  key_links:
    - from: ".planning/ROADMAP.md SC #2"
      to: "docker-compose.yml"
      via: "mem_limit values match actual services"
      pattern: "Neo4j 2GB.*Qdrant 1GB.*PostgreSQL 512MB.*n8n 512MB.*data-sidecar 512MB"
    - from: ".planning/ROADMAP.md SC #4"
      to: ".env.example"
      via: "GEMINI_API_KEY template entry is the deliverable"
      pattern: "env.example.*GEMINI_API_KEY"
---

<objective>
Close two verification gaps from 03-VERIFICATION.md by updating ROADMAP.md and REQUIREMENTS.md to accurately reflect Phase 3 scope.

Purpose: Phase 3 execution correctly deferred reasoning-engine service to Phase 8, but ROADMAP success criteria and REQUIREMENTS text still reference reasoning-engine as a Phase 3 deliverable. This mismatch causes verification failures. Aligning documentation with actual scope enables clean phase completion.

Output: Updated ROADMAP.md (SC #2, SC #4) and REQUIREMENTS.md (INFRA-03) with accurate Phase 3 scope boundaries.
</objective>

<execution_context>
@/Users/phananhle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phananhle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-infrastructure-hardening-and-database-migrations/03-VERIFICATION.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 3 Success Criteria #2 and #4</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit ROADMAP.md Phase 3 Success Criteria to close both verification gaps:

**Success Criterion #2 (mem_limit scope):**
Change the current text:
```
2. All Docker services in `docker-compose.yml` have explicit `mem_limit` values (Neo4j 2GB, Qdrant 1GB, PostgreSQL 512MB, n8n 512MB, reasoning-engine 2GB) — `docker inspect` confirms limits on running containers
```
To:
```
2. All existing Docker services in `docker-compose.yml` have explicit `mem_limit` values (Neo4j 2GB, Qdrant 1GB, PostgreSQL 512MB, n8n 512MB, data-sidecar 512MB) — `docker inspect` confirms limits on running containers. reasoning-engine `mem_limit: 2g` is set when the service is created in Phase 8.
```

Rationale: The reasoning-engine service does not exist until Phase 8. Plan 03-02 correctly deferred it. The 5 existing long-running services all have correct mem_limit values. data-sidecar 512MB was added per CONTEXT.md decision and should be listed.

**Success Criterion #4 (Gemini API key scope):**
Change the current text:
```
4. `GEMINI_API_KEY` is present in the environment configuration and accessible to the reasoning-engine service — a curl test against the Gemini API returns a valid response
```
To:
```
4. `GEMINI_API_KEY` is present in `.env.example` as a documented environment variable template — live API validation is a runtime verification item performed when the reasoning-engine service is deployed in Phase 8
```

Rationale: The `.env.example` template entry is the Phase 3 deliverable. The reasoning-engine service does not exist until Phase 8, so "accessible to the reasoning-engine service" and "curl test" are Phase 8 verification items.

Do NOT modify any other part of ROADMAP.md. Only these two success criteria lines.
  </action>
  <verify>
    <automated>grep -n "reasoning-engine.*Phase 8" .planning/ROADMAP.md | head -5</automated>
  </verify>
  <done>SC #2 lists only 5 existing services with data-sidecar and notes reasoning-engine deferred to Phase 8. SC #4 references .env.example as the deliverable and defers live API validation to Phase 8.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify REQUIREMENTS.md INFRA-03 scope</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit REQUIREMENTS.md INFRA-03 to clarify that reasoning-engine mem_limit is deferred:

Change the current text:
```
- [x] **INFRA-03**: Docker Compose has explicit `mem_limit` on all services (Neo4j 2GB, Qdrant 1GB, PostgreSQL 512MB, n8n 512MB, reasoning-engine 2GB)
```
To:
```
- [x] **INFRA-03**: Docker Compose has explicit `mem_limit` on all existing services (Neo4j 2GB, Qdrant 1GB, PostgreSQL 512MB, n8n 512MB, data-sidecar 512MB); reasoning-engine 2GB deferred to Phase 8 when service is created
```

Rationale: Aligns requirement text with actual Phase 3 scope. The 5 existing services are all correctly limited. reasoning-engine gets its mem_limit when SRVC-05 adds the service definition in Phase 8.

Do NOT modify any other part of REQUIREMENTS.md. Only the INFRA-03 line.
  </action>
  <verify>
    <automated>grep "INFRA-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>INFRA-03 lists 5 existing services including data-sidecar, notes reasoning-engine 2GB deferred to Phase 8.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "reasoning-engine" .planning/ROADMAP.md` — should show "Phase 8" in every mention within Phase 3 section
2. `grep "INFRA-03" .planning/REQUIREMENTS.md` — should show "deferred to Phase 8"
3. No other sections of ROADMAP.md or REQUIREMENTS.md are modified
4. All 6 INFRA requirement IDs still appear in the Phase 3 requirements line of ROADMAP.md
</verification>

<success_criteria>
- ROADMAP.md Phase 3 SC #2 lists 5 existing services (not 6) and explicitly defers reasoning-engine to Phase 8
- ROADMAP.md Phase 3 SC #4 references .env.example as the deliverable and defers live API test to Phase 8
- REQUIREMENTS.md INFRA-03 clarifies reasoning-engine mem_limit is deferred to Phase 8
- No functional changes — documentation-only scope alignment
</success_criteria>

<output>
After completion, create `.planning/phases/03-infrastructure-hardening-and-database-migrations/03-04-SUMMARY.md`
</output>
