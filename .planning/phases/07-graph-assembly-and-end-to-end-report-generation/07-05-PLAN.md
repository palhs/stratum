---
phase: 07-graph-assembly-and-end-to-end-report-generation
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - reasoning/app/pipeline/storage.py
  - reasoning/app/pipeline/__init__.py
  - reasoning/tests/pipeline/test_storage.py
  - reasoning/tests/pipeline/test_e2e.py
autonomous: true
requirements: [REPT-05, REAS-06, REPT-01, REPT-02, REPT-03, REPT-04]

must_haves:
  truths:
    - "write_report() inserts one row into the PostgreSQL reports table with asset_id, language, report_json (JSONB), report_markdown (TEXT), data_as_of, model_version, and generated_at"
    - "write_report() returns the generated report_id (BIGSERIAL primary key)"
    - "generate_report() calls prefetch(), then run_graph() twice (vi + en), then write_report() twice — producing 2 report rows"
    - "generate_report() uses copy.deepcopy(state) between vi and en invocations to prevent state mutation"
    - "A SELECT query against the reports table after generate_report() returns rows with correct asset_id, language, and non-null report_json"
    - "The full pipeline (prefetch → graph → compose → store) produces a valid bilingual report pair for a test asset"
  artifacts:
    - path: "reasoning/app/pipeline/storage.py"
      provides: "write_report() function — SQLAlchemy Core INSERT"
      exports: ["write_report"]
    - path: "reasoning/app/pipeline/__init__.py"
      provides: "generate_report() public entry point"
      exports: ["generate_report", "prefetch", "run_graph", "build_graph"]
    - path: "reasoning/tests/pipeline/test_storage.py"
      provides: "Unit tests for report storage"
    - path: "reasoning/tests/pipeline/test_e2e.py"
      provides: "End-to-end integration test (requires Docker services)"
  key_links:
    - from: "reasoning/app/pipeline/storage.py"
      to: "reasoning/app/models/tables.py"
      via: "SQLAlchemy Table reference or raw text INSERT for reports table"
      pattern: "from sqlalchemy import"
    - from: "reasoning/app/pipeline/__init__.py"
      to: "reasoning/app/pipeline/storage.py"
      via: "import write_report"
      pattern: "from reasoning.app.pipeline.storage import write_report"
    - from: "reasoning/app/pipeline/__init__.py"
      to: "reasoning/app/pipeline/graph.py"
      via: "import run_graph"
      pattern: "from reasoning.app.pipeline.graph import run_graph"
    - from: "reasoning/app/pipeline/__init__.py"
      to: "reasoning/app/pipeline/prefetch.py"
      via: "import prefetch"
      pattern: "from reasoning.app.pipeline.prefetch import prefetch"
---

<objective>
Implement PostgreSQL report storage, the generate_report() public entry point, and an end-to-end integration test that validates the complete pipeline from prefetch through graph execution to database storage.

Purpose: Satisfy REPT-05 (reports stored in PostgreSQL) and validate the complete Phase 7 pipeline end-to-end. This is the final integration plan — generate_report() is the function Phase 8 FastAPI will call. All 6 phase requirements (REAS-06, REPT-01 through REPT-05) are verified by the E2E test.

Output: storage.py (write_report), updated __init__.py (generate_report public API), and E2E integration test.
</objective>

<execution_context>
@/Users/phananhle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phananhle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-graph-assembly-and-end-to-end-report-generation/07-CONTEXT.md
@.planning/phases/07-graph-assembly-and-end-to-end-report-generation/07-RESEARCH.md
@.planning/phases/07-graph-assembly-and-end-to-end-report-generation/07-04-SUMMARY.md
@reasoning/app/pipeline/compose_report.py
@reasoning/app/pipeline/graph.py
@reasoning/app/pipeline/prefetch.py
@db/migrations/V6__reports.sql

<interfaces>
<!-- PostgreSQL reports table schema -->

From db/migrations/V6__reports.sql:
```sql
CREATE TABLE reports (
    report_id             BIGSERIAL    PRIMARY KEY,
    asset_id              VARCHAR(20)  NOT NULL,
    language              VARCHAR(5)   NOT NULL CHECK (language IN ('vi', 'en')),
    report_json           JSONB        NOT NULL,
    report_markdown       TEXT,
    data_as_of            TIMESTAMPTZ  NOT NULL,
    model_version         VARCHAR(100),
    pipeline_duration_ms  INTEGER,
    generated_at          TIMESTAMPTZ  NOT NULL DEFAULT NOW(),
    ingested_at           TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);
```

From reasoning/app/nodes/state.py:
```python
class ReportOutput(BaseModel):
    report_json: dict
    report_markdown: str
    language: str
    data_as_of: datetime
    data_warnings: list[str]
    model_version: str = "gemini-2.5-pro"
    warnings: list[str] = []
```

Established SQLAlchemy Core pattern (from reasoning/app/retrieval/postgres_retriever.py):
```python
from sqlalchemy import create_engine, text
# Uses engine.connect() + conn.execute(text(...)) pattern
```

Pipeline public API (from pipeline/__init__.py):
```python
async def generate_report(ticker, asset_type, db_engine, neo4j_driver, qdrant_client, db_uri) -> tuple[int, int]:
    # Returns (vi_report_id, en_report_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostgreSQL report storage and generate_report entry point</name>
  <files>
    reasoning/app/pipeline/storage.py,
    reasoning/app/pipeline/__init__.py,
    reasoning/tests/pipeline/test_storage.py
  </files>
  <behavior>
    - Test: write_report() with mock db_engine inserts correct values (asset_id, language, report_json, report_markdown, data_as_of, model_version, generated_at)
    - Test: write_report() returns an integer report_id
    - Test: write_report() uses json.loads(report_output.model_dump_json()) for report_json to avoid Pydantic serialization issues with JSONB
    - Test: write_report() records pipeline_duration_ms when provided
    - Test: generate_report() calls prefetch once, run_graph twice (vi then en), write_report twice
    - Test: generate_report() uses copy.deepcopy between vi and en invocations (mock deepcopy to verify call)
    - Test: generate_report() returns tuple of two report IDs
  </behavior>
  <action>
    **Step 1: Create storage.py**
    Implement `write_report(db_engine, asset_id: str, language: str, report_output: ReportOutput, pipeline_duration_ms: Optional[int] = None) -> int`:
    - Use SQLAlchemy Core: `from sqlalchemy import Table, MetaData, insert`
    - Reflect reports table: `Table("reports", MetaData(), autoload_with=db_engine)`
    - Build values dict:
      - `asset_id=asset_id`
      - `language=language`
      - `report_json=report_output.report_json` (already a plain dict from model_dump)
      - `report_markdown=report_output.report_markdown`
      - `data_as_of=report_output.data_as_of`
      - `model_version=report_output.model_version`
      - `pipeline_duration_ms=pipeline_duration_ms`
      - `generated_at=datetime.now(timezone.utc)`
    - Execute INSERT with `.returning(reports_table.c.report_id)`
    - `conn.commit()` and return `result.scalar_one()`

    **Step 2: Create generate_report() in __init__.py**
    ```python
    async def generate_report(
        ticker: str,
        asset_type: str,
        db_engine,
        neo4j_driver,
        qdrant_client,
        db_uri: str,
    ) -> tuple[int, int]:
    ```
    - Call `prefetch(ticker, asset_type, db_engine, neo4j_driver, qdrant_client)`
    - `import copy, uuid, time`
    - Vietnamese invocation:
      - `state_vi = copy.deepcopy(state)`
      - `thread_id_vi = f"{ticker}-vi-{uuid.uuid4()}"`
      - `start = time.monotonic()`
      - `result_vi = await run_graph(state_vi, "vi", thread_id_vi, db_uri)`
      - `duration_vi = int((time.monotonic() - start) * 1000)`
      - `vi_id = write_report(db_engine, ticker, "vi", result_vi["report_output"], duration_vi)`
    - English invocation: same pattern with "en"
    - Return `(vi_id, en_id)`

    Update __init__.py exports: `__all__ = ["generate_report", "prefetch", "run_graph", "build_graph"]`

    **Step 3: Write storage tests**
    Mock the SQLAlchemy engine and verify INSERT parameters. Test that report_json is a plain dict (not Pydantic model). Test pipeline_duration_ms is passed through.
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum && python -m pytest reasoning/tests/pipeline/test_storage.py -x -v</automated>
  </verify>
  <done>
    - write_report() inserts correct values into reports table and returns report_id
    - generate_report() orchestrates prefetch → run_graph(vi) → write → run_graph(en) → write
    - Deep copy prevents state mutation between language invocations
    - Pipeline duration tracked in milliseconds
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test</name>
  <files>
    reasoning/tests/pipeline/test_e2e.py
  </files>
  <action>
    Create reasoning/tests/pipeline/test_e2e.py — a comprehensive integration test that validates the full pipeline.

    **Test design:**
    - Mark all tests with `@pytest.mark.integration` so they can be skipped in CI (require Docker services)
    - Mark async tests with `@pytest.mark.asyncio`
    - Use mock Gemini responses (patch ChatGoogleGenerativeAI) — do NOT make live API calls
    - Use mock retrieval functions (patch all retrieval imports in prefetch.py) — do NOT require live DB connections
    - Use a mock db_engine for write_report (patch SQLAlchemy execute)
    - Use a mock AsyncPostgresSaver (patch from_conn_string) — do NOT require live PostgreSQL

    **Test cases:**
    1. `test_e2e_equity_vi_en`: Full pipeline for VHM equity asset
       - Mock all retrieval functions to return realistic test data
       - Mock Gemini to return valid structured output for each node
       - Call `generate_report("VHM", "equity", mock_engine, mock_neo4j, mock_qdrant, "postgresql://...")`
       - Assert 2 write_report calls (vi + en)
       - Assert report_json has all 4 card sections
       - Assert report_markdown is non-empty for both languages
       - Assert report_json["data_warnings"] is a list

    2. `test_e2e_gold_with_wgc_warning`: Full pipeline for gold asset
       - Same mock pattern
       - Call `generate_report("GOLD", "gold", ...)`
       - Assert report_json["data_warnings"] contains WGC warning string
       - Assert 2 write_report calls

    3. `test_e2e_report_json_schema_validation`: Validate report_json structure
       - After pipeline run, load report_json into ReportCard Pydantic model
       - Assert no ValidationError — all required fields present
       - Assert entry_quality card has tier, narrative, assessments
       - Assert macro_regime card has label, confidence, narrative
       - Assert valuation card has label, narrative
       - Assert structure card has label, narrative

    4. `test_e2e_no_prohibited_terms_in_markdown`: Scan Markdown output
       - Run pipeline, get report_markdown
       - Assert no standalone "buy", "sell", "entry confirmed" in English output
       - Assert no "mua vào", "bán", "xác nhận điểm vào" in Vietnamese output
       - Allow compound words: "buyback", "sell-off", "oversold", "overbought"

    5. `test_e2e_bilingual_independence`: Verify vi and en are independent
       - Run pipeline, capture both write_report calls
       - Assert vi report_json labels are Vietnamese (from term dict)
       - Assert vi report_json narratives are Vietnamese (Gemini re-generated, not English)
       - Assert en report_json labels are English
       - Assert en report_json narratives are English (original Phase 6 output)
       - Assert vi and en report_markdown are different strings

    **Important mock patterns:**
    - Patch `reasoning.app.pipeline.prefetch.get_fundamentals` (not the retrieval module — patch where it's used)
    - Patch `reasoning.app.pipeline.graph.AsyncPostgresSaver` to return a mock checkpointer
    - The mock checkpointer's `__aenter__` returns itself; `aput`, `aget` are no-ops
    - Each Phase 6 node must be patched or allowed to run with mock data — prefer patching nodes to return predetermined outputs for test speed
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum && python -m pytest reasoning/tests/pipeline/test_e2e.py -x -v -m "not integration"</automated>
  </verify>
  <done>
    - E2E test validates complete pipeline from prefetch through storage
    - Both equity (VHM) and gold paths tested
    - Bilingual independence verified (vi and en are different)
    - JSON schema validates with Pydantic
    - No prohibited terms in Markdown
    - DATA WARNING present for gold assets
    - All tests pass with mocked infrastructure
  </done>
</task>

</tasks>

<verification>
- `python -m pytest reasoning/tests/pipeline/ -x -v` ALL pipeline tests pass
- `python -c "from reasoning.app.pipeline import generate_report, prefetch, run_graph, build_graph"` succeeds
- `python -c "from reasoning.app.pipeline.storage import write_report"` succeeds
- Total Phase 7 test count is significant (>= 30 tests across all test files)
</verification>

<success_criteria>
- Reports stored in PostgreSQL with correct schema (asset_id, language, report_json, report_markdown, data_as_of, model_version)
- generate_report() is the single public entry point for Phase 8 FastAPI
- E2E test validates all 6 Phase 7 requirements (REAS-06, REPT-01 through REPT-05)
- Bilingual reports are independently generated (not translated)
- Deep copy prevents state mutation between language invocations
- Pipeline duration tracked
</success_criteria>

<output>
After completion, create `.planning/phases/07-graph-assembly-and-end-to-end-report-generation/07-05-SUMMARY.md`
</output>
