---
phase: 08.1-docker-runtime-fixes
plan: "01"
subsystem: infra
tags: [docker, dockerfile, python-imports, langgraph, docker-compose]

# Dependency graph
requires:
  - phase: 08-fastapi-gateway-and-docker-service
    provides: reasoning-engine FastAPI service and docker-compose service definition
provides:
  - Dockerfile with /app/reasoning/app/ COPY layout matching from reasoning.app.* absolute import namespace
  - reasoning/__init__.py package marker inside container
  - docker-compose.yml reasoning-engine depends_on langgraph-init with service_completed_successfully
affects: [09-e2e-integration, phase-9]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dockerfile COPY app/ → /app/reasoning/app/ with reasoning/__init__.py marker — creates Python package namespace matching absolute imports"
    - "CMD uses reasoning.app.main:app — consistent with from reasoning.app.* import chain"
    - "depends_on service_completed_successfully — startup ordering for schema-dependent services"

key-files:
  created: []
  modified:
    - reasoning/Dockerfile
    - docker-compose.yml

key-decisions:
  - "Dockerfile restructured COPY layout: COPY app/ ./reasoning/app/ with RUN mkdir -p reasoning && touch reasoning/__init__.py — makes /app/reasoning a Python package, allowing from reasoning.app.* imports to resolve when /app is on sys.path"
  - "CMD updated from app.main:app to reasoning.app.main:app — aligns uvicorn entrypoint with the import namespace used throughout the codebase"
  - "langgraph-init added to reasoning-engine depends_on with condition: service_completed_successfully — ensures LangGraph checkpoint schema tables exist before reasoning-engine starts"

patterns-established:
  - "Python import namespace in Docker: WORKDIR /app + COPY into /app/reasoning/app/ + reasoning/__init__.py = from reasoning.app.* resolves without PYTHONPATH hacks"

requirements-completed: []

# Metrics
duration: ~10min
completed: 2026-03-16
---

# Phase 8.1 Plan 01: Docker Runtime Fixes Summary

**Patched reasoning-engine Dockerfile and docker-compose.yml so `from reasoning.app.*` imports resolve inside the container and service starts after langgraph-init completes**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-16T09:35:00Z
- **Completed:** 2026-03-16T11:13:26Z
- **Tasks:** 2 (1 auto + 1 human-verify checkpoint)
- **Files modified:** 2

## Accomplishments

- Fixed the root cause of all `ModuleNotFoundError: No module named 'reasoning'` failures inside the container — COPY layout now places code at `/app/reasoning/app/` with a `reasoning/__init__.py` package marker
- Updated uvicorn CMD entrypoint from `app.main:app` to `reasoning.app.main:app` to match the absolute import namespace
- Added `langgraph-init` to `reasoning-engine` depends_on with `condition: service_completed_successfully` — prevents startup races with checkpoint schema creation
- Human verification confirmed: Docker build succeeded (image 84727ca6ba36), `python -c "from reasoning.app.pipeline import generate_report; print('OK')"` printed OK inside container

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix Docker import path and add langgraph-init dependency** - `17296c1` (feat)
2. **Task 2: Verify E2E import resolution inside container** - human-verify checkpoint, approved by user

## Files Created/Modified

- `reasoning/Dockerfile` - Restructured COPY layout: `COPY app/ ./reasoning/app/` with `reasoning/__init__.py` package marker; CMD updated to `reasoning.app.main:app`
- `docker-compose.yml` - Added `langgraph-init: condition: service_completed_successfully` to reasoning-engine depends_on block

## Decisions Made

- Dockerfile COPY restructure is the minimal fix: no PYTHONPATH env var, no `pip install -e .`, just an `__init__.py` marker and correct COPY destination. Python's default sys.path includes CWD (`/app`), so `import reasoning` finds `/app/reasoning/` automatically.
- Full-stack E2E test (POST /reports/generate end-to-end) deferred to Phase 9 per user decision — import resolution and startup ordering are the two blocking issues; live pipeline test requires full infrastructure.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — Task 1 applied cleanly, Task 2 human verification confirmed all three must_have truths:
1. Docker build succeeded
2. Import test printed OK
3. docker compose config confirmed langgraph-init in depends_on with service_completed_successfully

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- reasoning-engine container can now import and run pipeline code — blocking integration issue from v2.0-MILESTONE-AUDIT.md is closed
- Full-stack E2E test (Phase 9) can proceed: POST /reports/generate with live Postgres, Neo4j, Qdrant, and Gemini API
- Two v2.0 integration issues from audit are resolved: import namespace (this plan) and checkpoint schema ordering (this plan)

---
*Phase: 08.1-docker-runtime-fixes*
*Completed: 2026-03-16*
