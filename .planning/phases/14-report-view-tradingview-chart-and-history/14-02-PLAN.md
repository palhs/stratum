---
phase: 14-report-view-tradingview-chart-and-history
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - frontend/src/components/report/ReportSummaryCard.tsx
  - frontend/src/components/report/BilingualToggle.tsx
  - frontend/src/components/report/TradingViewChart.tsx
  - frontend/src/components/report/ReportMarkdown.tsx
  - frontend/src/components/report/ReportPageSkeleton.tsx
  - frontend/src/components/report/__tests__/ReportSummaryCard.test.tsx
  - frontend/src/components/report/__tests__/BilingualToggle.test.tsx
  - frontend/src/components/report/__tests__/TradingViewChart.test.tsx
autonomous: true
requirements: [RVEW-01, RVEW-03, RVEW-04]

must_haves:
  truths:
    - "ReportSummaryCard renders tier badge at display size, three sub-assessment labels, and one-line verdict"
    - "BilingualToggle floats fixed top-right, persists language preference in localStorage, defaults to vi"
    - "TradingViewChart renders weekly candlestick + MA50 + MA200 lines without SSR crash"
    - "ReportMarkdown renders markdown content with prose typography styling"
  artifacts:
    - path: "frontend/src/components/report/ReportSummaryCard.tsx"
      provides: "Summary card with tier hero, sub-assessments, verdict, expand button"
      contains: "ReportSummaryCard"
    - path: "frontend/src/components/report/BilingualToggle.tsx"
      provides: "Floating language toggle"
      contains: "BilingualToggle"
    - path: "frontend/src/components/report/TradingViewChart.tsx"
      provides: "TradingView Lightweight Charts candlestick wrapper"
      contains: "createChart"
    - path: "frontend/src/components/report/ReportMarkdown.tsx"
      provides: "Markdown renderer with prose styling"
      contains: "ReactMarkdown"
    - path: "frontend/src/components/report/ReportPageSkeleton.tsx"
      provides: "Loading skeleton for report page"
      contains: "ReportPageSkeleton"
  key_links:
    - from: "frontend/src/components/report/ReportSummaryCard.tsx"
      to: "frontend/src/components/dashboard/TierBadge.tsx"
      via: "TierBadge import"
      pattern: "import.*TierBadge.*from.*dashboard/TierBadge"
    - from: "frontend/src/components/report/TradingViewChart.tsx"
      to: "lightweight-charts"
      via: "createChart import"
      pattern: "import.*createChart.*from.*lightweight-charts"
    - from: "frontend/src/components/report/ReportMarkdown.tsx"
      to: "react-markdown"
      via: "ReactMarkdown import"
      pattern: "import.*ReactMarkdown.*from.*react-markdown"
---

<objective>
Build all leaf UI components for the report page: ReportSummaryCard, BilingualToggle, TradingViewChart, ReportMarkdown, and ReportPageSkeleton. Each is a self-contained "use client" component with unit tests.

Purpose: These components are the visual building blocks consumed by the ReportPageClient orchestrator in Plan 03. Building and testing them independently ensures each requirement (RVEW-01, RVEW-03, RVEW-04) is verified before integration.

Output: Five tested React components in frontend/src/components/report/.
</objective>

<execution_context>
@/Users/phananhle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phananhle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-report-view-tradingview-chart-and-history/14-CONTEXT.md
@.planning/phases/14-report-view-tradingview-chart-and-history/14-RESEARCH.md
@.planning/phases/14-report-view-tradingview-chart-and-history/14-UI-SPEC.md
@.planning/phases/14-report-view-tradingview-chart-and-history/14-01-SUMMARY.md

<interfaces>
<!-- Existing components and types to use -->

From frontend/src/components/dashboard/TierBadge.tsx:
```typescript
export const TIER_STYLES: Record<string, string> = {
  Favorable: 'bg-teal-100 text-teal-800 border-teal-200 dark:bg-teal-900/30 dark:text-teal-300',
  Neutral:   'bg-slate-100 text-slate-700 border-slate-200 dark:bg-slate-800 dark:text-slate-300',
  Cautious:  'bg-amber-100 text-amber-800 border-amber-200 dark:bg-amber-900/30 dark:text-amber-300',
  Avoid:     'bg-rose-100 text-rose-800 border-rose-200 dark:bg-rose-900/30 dark:text-rose-300',
}
export function TierBadge({ tier }: { tier: string | null | undefined })
```

From frontend/src/lib/types.ts (after Plan 01):
```typescript
export interface OHLCVPoint {
  time: number
  close: number
  open?: number | null
  high?: number | null
  low?: number | null
  volume?: number | null
  ma50?: number | null
  ma200?: number | null
}

export interface ReportContentResponse {
  report_id: number
  generated_at: string
  tier: string
  verdict: string
  macro_assessment: string
  valuation_assessment: string
  structure_assessment: string
  report_markdown_vi: string | null
  report_markdown_en: string | null
}
```

From frontend/src/app/globals.css (after Plan 01):
```css
@import "tailwindcss";
@plugin "@tailwindcss/typography";
```

From frontend/src/components/ui/button.tsx:
```typescript
// shadcn Button with variant="ghost", variant="outline", variant="default"
```

From frontend/src/components/ui/skeleton.tsx:
```typescript
// shadcn Skeleton component
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReportSummaryCard and BilingualToggle components with tests</name>
  <files>
    frontend/src/components/report/ReportSummaryCard.tsx,
    frontend/src/components/report/BilingualToggle.tsx,
    frontend/src/components/report/__tests__/ReportSummaryCard.test.tsx,
    frontend/src/components/report/__tests__/BilingualToggle.test.tsx
  </files>
  <read_first>
    frontend/src/components/dashboard/TierBadge.tsx,
    frontend/src/lib/types.ts,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/card.tsx,
    frontend/src/app/globals.css
  </read_first>
  <behavior>
    ReportSummaryCard:
    - Renders TierBadge with tier prop at display size
    - Renders three sub-assessment labels: "Macro", "Valuation", "Structure" with their values
    - Renders one-line verdict text
    - Has expand/collapse button with text "Read full report" / "Collapse report"
    - Calls onExpandToggle callback when expand button is clicked
    - Shows ChevronDown icon when collapsed, ChevronUp when expanded

    BilingualToggle:
    - Renders "VI" and "EN" buttons
    - Active language button uses variant="default", inactive uses variant="outline"
    - Calls onLanguageChange callback with 'vi' or 'en' when clicked
    - Has fixed positioning classes (fixed top-4 right-4)
    - Has aria-label="Report language" on container
    - Each button has aria-pressed matching active state
  </behavior>
  <action>
1. Create frontend/src/components/report/__tests__/ReportSummaryCard.test.tsx:
- Test renders TierBadge with given tier ("Favorable")
- Test renders sub-assessment labels: screen.getByText("Macro"), screen.getByText("Valuation"), screen.getByText("Structure")
- Test renders verdict text
- Test expand button shows "Read full report" when isExpanded=false
- Test expand button shows "Collapse report" when isExpanded=true
- Test clicking expand button calls onExpandToggle

2. Create frontend/src/components/report/__tests__/BilingualToggle.test.tsx:
- Test renders VI and EN buttons
- Test VI button has aria-pressed="true" when lang="vi"
- Test EN button has aria-pressed="true" when lang="en"
- Test clicking EN button calls onLanguageChange with "en"
- Test has aria-label="Report language"
- Test has fixed positioning (check className contains "fixed")

3. Create frontend/src/components/report/ReportSummaryCard.tsx:
```typescript
'use client'

import { Card } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { TierBadge } from '@/components/dashboard/TierBadge'
import { ChevronDown, ChevronUp } from 'lucide-react'

interface ReportSummaryCardProps {
  tier: string
  verdict: string
  macroAssessment: string
  valuationAssessment: string
  structureAssessment: string
  isExpanded: boolean
  onExpandToggle: () => void
}

export function ReportSummaryCard({
  tier, verdict, macroAssessment, valuationAssessment, structureAssessment,
  isExpanded, onExpandToggle,
}: ReportSummaryCardProps) {
  return (
    <Card className="p-4">
      <div className="flex flex-col gap-4">
        <TierBadge tier={tier} />
        <div className="flex flex-wrap gap-4">
          {[
            { label: 'Macro', value: macroAssessment },
            { label: 'Valuation', value: valuationAssessment },
            { label: 'Structure', value: structureAssessment },
          ].map(({ label, value }) => (
            <div key={label} className="flex items-center gap-1">
              <span className="text-sm text-muted-foreground">{label}</span>
              <span className="text-sm text-foreground">{value}</span>
            </div>
          ))}
        </div>
        <p className="text-base leading-relaxed">{verdict}</p>
        <Button
          variant="ghost"
          className="w-full"
          onClick={onExpandToggle}
          aria-expanded={isExpanded}
        >
          {isExpanded ? 'Collapse report' : 'Read full report'}
          {isExpanded ? <ChevronUp className="ml-1 h-4 w-4" /> : <ChevronDown className="ml-1 h-4 w-4" />}
        </Button>
      </div>
    </Card>
  )
}
```

4. Create frontend/src/components/report/BilingualToggle.tsx:
```typescript
'use client'

import { Button } from '@/components/ui/button'

interface BilingualToggleProps {
  lang: 'vi' | 'en'
  onLanguageChange: (lang: 'vi' | 'en') => void
}

export function BilingualToggle({ lang, onLanguageChange }: BilingualToggleProps) {
  return (
    <div className="fixed top-4 right-4 z-50 flex gap-1" aria-label="Report language" role="group">
      <Button
        variant={lang === 'vi' ? 'default' : 'outline'}
        size="sm"
        className="px-4 py-2 text-sm"
        onClick={() => onLanguageChange('vi')}
        aria-pressed={lang === 'vi'}
      >
        VI
      </Button>
      <Button
        variant={lang === 'en' ? 'default' : 'outline'}
        size="sm"
        className="px-4 py-2 text-sm"
        onClick={() => onLanguageChange('en')}
        aria-pressed={lang === 'en'}
      >
        EN
      </Button>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum/frontend && npm test -- --reporter=verbose src/components/report/__tests__/ReportSummaryCard.test.tsx src/components/report/__tests__/BilingualToggle.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/report/ReportSummaryCard.tsx contains `export function ReportSummaryCard(`
    - frontend/src/components/report/ReportSummaryCard.tsx contains `import { TierBadge } from '@/components/dashboard/TierBadge'`
    - frontend/src/components/report/ReportSummaryCard.tsx contains `aria-expanded={isExpanded}`
    - frontend/src/components/report/ReportSummaryCard.tsx contains the string "Read full report"
    - frontend/src/components/report/ReportSummaryCard.tsx contains the string "Collapse report"
    - frontend/src/components/report/ReportSummaryCard.tsx contains "Macro" and "Valuation" and "Structure" as sub-assessment labels
    - frontend/src/components/report/BilingualToggle.tsx contains `export function BilingualToggle(`
    - frontend/src/components/report/BilingualToggle.tsx contains `className="fixed top-4 right-4`
    - frontend/src/components/report/BilingualToggle.tsx contains `aria-label="Report language"`
    - frontend/src/components/report/BilingualToggle.tsx contains `aria-pressed={lang ===`
    - Tests exit 0
  </acceptance_criteria>
  <done>ReportSummaryCard renders tier badge, three sub-assessments, verdict, and expand/collapse button. BilingualToggle floats fixed top-right with VI/EN buttons and aria-pressed state. Both have passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TradingViewChart, ReportMarkdown, and ReportPageSkeleton components with tests</name>
  <files>
    frontend/src/components/report/TradingViewChart.tsx,
    frontend/src/components/report/ReportMarkdown.tsx,
    frontend/src/components/report/ReportPageSkeleton.tsx,
    frontend/src/components/report/__tests__/TradingViewChart.test.tsx
  </files>
  <read_first>
    frontend/src/lib/types.ts,
    frontend/src/components/ui/skeleton.tsx,
    frontend/src/app/globals.css
  </read_first>
  <behavior>
    TradingViewChart:
    - Calls createChart from lightweight-charts with container ref
    - Creates candlestick series, MA50 line series (blue), MA200 line series (orange)
    - Sets visible range to last 52 data points
    - Cleans up chart on unmount via chart.remove()
    - Container has h-[260px] md:h-[400px] classes for responsive height
    - Has aria-label containing "Price chart"

    ReportMarkdown:
    - Renders markdown content using ReactMarkdown with remarkGfm plugin
    - Wraps content in article element with lang attribute
    - Uses prose prose-zinc dark:prose-invert max-w-none classes

    ReportPageSkeleton:
    - Renders skeleton placeholders for summary card, chart, and timeline
  </behavior>
  <action>
1. Create frontend/src/components/report/__tests__/TradingViewChart.test.tsx:
- Mock lightweight-charts: vi.mock('lightweight-charts', ...) returning mock createChart that returns mock chart object with addCandlestickSeries, addLineSeries, addHistogramSeries, timeScale, applyOptions, remove methods
- Test createChart is called when data is non-empty
- Test chart.remove() is called on unmount
- Test container has aria-label containing "Price chart"
- Test container has responsive height classes

2. Create frontend/src/components/report/TradingViewChart.tsx — "use client" component:
```typescript
'use client'

import { useEffect, useRef } from 'react'
import { createChart, ColorType, CrosshairMode } from 'lightweight-charts'
import type { OHLCVPoint } from '@/lib/types'
import type { UTCTimestamp } from 'lightweight-charts'

export default function TradingViewChart({ data }: { data: OHLCVPoint[] }) {
  const containerRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    if (!containerRef.current || data.length === 0) return

    const chart = createChart(containerRef.current, {
      layout: {
        background: { type: ColorType.Solid, color: 'transparent' },
        textColor: '#71717a',
      },
      grid: {
        vertLines: { color: '#27272a' },
        horzLines: { color: '#27272a' },
      },
      crosshair: { mode: CrosshairMode.Normal },
      width: containerRef.current.clientWidth,
      height: containerRef.current.clientHeight,
    })

    const candlestickSeries = chart.addCandlestickSeries({
      upColor: '#14b8a6',
      downColor: '#f43f5e',
      borderVisible: false,
      wickUpColor: '#14b8a6',
      wickDownColor: '#f43f5e',
    })

    const ma50Series = chart.addLineSeries({
      color: '#3b82f6',
      lineWidth: 2,
      priceLineVisible: false,
    })

    const ma200Series = chart.addLineSeries({
      color: '#f97316',
      lineWidth: 2,
      priceLineVisible: false,
    })

    // Volume histogram on secondary price scale
    const volumeSeries = chart.addHistogramSeries({
      color: 'rgba(113, 113, 122, 0.3)',
      priceFormat: { type: 'volume' },
      priceScaleId: 'volume',
    })
    chart.priceScale('volume').applyOptions({
      scaleMargins: { top: 0.8, bottom: 0 },
    })

    const candleData = data.map(p => ({
      time: p.time as UTCTimestamp,
      open: p.open ?? p.close,
      high: p.high ?? p.close,
      low: p.low ?? p.close,
      close: p.close,
    }))

    const ma50Data = data
      .filter(p => p.ma50 != null)
      .map(p => ({ time: p.time as UTCTimestamp, value: p.ma50! }))

    const ma200Data = data
      .filter(p => p.ma200 != null)
      .map(p => ({ time: p.time as UTCTimestamp, value: p.ma200! }))

    const volumeData = data
      .filter(p => p.volume != null)
      .map(p => ({ time: p.time as UTCTimestamp, value: p.volume! }))

    candlestickSeries.setData(candleData)
    ma50Series.setData(ma50Data)
    ma200Series.setData(ma200Data)
    volumeSeries.setData(volumeData)

    if (data.length > 52) {
      chart.timeScale().setVisibleRange({
        from: data[data.length - 52].time as UTCTimestamp,
        to: data[data.length - 1].time as UTCTimestamp,
      })
    }

    const handleResize = () => {
      if (containerRef.current) {
        chart.applyOptions({ width: containerRef.current.clientWidth })
      }
    }
    window.addEventListener('resize', handleResize)

    return () => {
      window.removeEventListener('resize', handleResize)
      chart.remove()
    }
  }, [data])

  return (
    <div
      ref={containerRef}
      className="w-full h-[260px] md:h-[400px] rounded-lg overflow-hidden border border-border"
      aria-label="Price chart"
    />
  )
}
```

3. Create frontend/src/components/report/ReportMarkdown.tsx:
```typescript
'use client'

import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

interface ReportMarkdownProps {
  content: string
  lang: 'vi' | 'en'
}

export function ReportMarkdown({ content, lang }: ReportMarkdownProps) {
  return (
    <article lang={lang} className="prose prose-zinc dark:prose-invert max-w-none">
      <ReactMarkdown remarkPlugins={[remarkGfm]}>
        {content}
      </ReactMarkdown>
    </article>
  )
}
```

4. Create frontend/src/components/report/ReportPageSkeleton.tsx:
```typescript
import { Skeleton } from '@/components/ui/skeleton'

export function ReportPageSkeleton() {
  return (
    <div className="flex flex-col gap-6">
      <Skeleton className="w-full h-32 rounded-lg" />
      <Skeleton className="w-full h-[260px] md:h-[400px] rounded-lg" />
      <div className="flex flex-col gap-2">
        <Skeleton className="h-11 w-full rounded" />
        <Skeleton className="h-11 w-full rounded" />
        <Skeleton className="h-11 w-full rounded" />
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum/frontend && npm test -- --reporter=verbose src/components/report/__tests__/TradingViewChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/report/TradingViewChart.tsx contains `export default function TradingViewChart(`
    - frontend/src/components/report/TradingViewChart.tsx contains `import { createChart, ColorType, CrosshairMode } from 'lightweight-charts'`
    - frontend/src/components/report/TradingViewChart.tsx contains `addCandlestickSeries`
    - frontend/src/components/report/TradingViewChart.tsx contains `addLineSeries` (for MA50 and MA200)
    - frontend/src/components/report/TradingViewChart.tsx contains `addHistogramSeries` (for volume)
    - frontend/src/components/report/TradingViewChart.tsx contains `chart.remove()` in cleanup
    - frontend/src/components/report/TradingViewChart.tsx contains `aria-label="Price chart"`
    - frontend/src/components/report/TradingViewChart.tsx contains `h-[260px] md:h-[400px]`
    - frontend/src/components/report/ReportMarkdown.tsx contains `import ReactMarkdown from 'react-markdown'`
    - frontend/src/components/report/ReportMarkdown.tsx contains `import remarkGfm from 'remark-gfm'`
    - frontend/src/components/report/ReportMarkdown.tsx contains `prose prose-zinc dark:prose-invert max-w-none`
    - frontend/src/components/report/ReportMarkdown.tsx contains `<article lang={lang}`
    - frontend/src/components/report/ReportPageSkeleton.tsx contains `export function ReportPageSkeleton(`
    - TradingViewChart test exits 0
    - Full test suite: `npm test` exits 0
  </acceptance_criteria>
  <done>TradingViewChart renders candlestick + MA50 + MA200 + volume histogram with proper cleanup. ReportMarkdown renders prose-styled markdown with lang attribute. ReportPageSkeleton provides loading state. All tests pass.</done>
</task>

</tasks>

<verification>
- All component tests pass: `cd frontend && npm test -- --reporter=verbose src/components/report/__tests__/`
- TypeScript compiles: `cd frontend && npx tsc --noEmit`
- Full test suite green: `cd frontend && npm test`
</verification>

<success_criteria>
- ReportSummaryCard renders tier badge, Macro/Valuation/Structure labels, verdict, expand button with correct copy
- BilingualToggle floats fixed top-4 right-4 with VI/EN buttons, aria-pressed, aria-label
- TradingViewChart renders candlestick + MA50 (blue) + MA200 (orange) + volume histogram, cleans up on unmount
- ReportMarkdown renders markdown with prose typography and lang attribute
- ReportPageSkeleton renders loading placeholders
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-report-view-tradingview-chart-and-history/14-02-SUMMARY.md`
</output>
