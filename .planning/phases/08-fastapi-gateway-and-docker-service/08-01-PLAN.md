---
phase: 08-fastapi-gateway-and-docker-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reasoning/app/main.py
  - reasoning/app/dependencies.py
  - reasoning/app/routers/__init__.py
  - reasoning/app/routers/health.py
  - reasoning/Dockerfile
  - reasoning/requirements.txt
  - docker-compose.yml
  - reasoning/tests/api/__init__.py
  - reasoning/tests/api/test_health.py
autonomous: true
requirements: [SRVC-04, SRVC-05]

must_haves:
  truths:
    - "GET /health returns 200 with service status"
    - "reasoning-engine service defined in docker-compose.yml with profiles: [reasoning], mem_limit: 2g, and correct depends_on"
    - "Dockerfile builds a working container image from reasoning/ directory"
    - "requirements.txt includes sse-starlette, langgraph-checkpoint-postgres, psycopg[binary], and fastapi/uvicorn"
  artifacts:
    - path: "reasoning/app/main.py"
      provides: "FastAPI app with lifespan, router registration"
      contains: "lifespan"
    - path: "reasoning/app/dependencies.py"
      provides: "Lifespan context manager initializing db_engine, neo4j_driver, qdrant_client, job_queues"
      contains: "asynccontextmanager"
    - path: "reasoning/app/routers/health.py"
      provides: "GET /health endpoint"
      contains: "router"
    - path: "reasoning/Dockerfile"
      provides: "Docker container packaging"
      contains: "uvicorn"
    - path: "docker-compose.yml"
      provides: "reasoning-engine service definition"
      contains: "reasoning-engine"
    - path: "reasoning/tests/api/test_health.py"
      provides: "Health endpoint test"
      contains: "test_health"
  key_links:
    - from: "reasoning/app/main.py"
      to: "reasoning/app/routers/health.py"
      via: "include_router()"
      pattern: "include_router.*health"
    - from: "docker-compose.yml"
      to: "reasoning/Dockerfile"
      via: "build context"
      pattern: "context.*reasoning"
---

<objective>
Create the FastAPI application skeleton for the reasoning-engine service with a health endpoint, Dockerfile, and docker-compose.yml service definition.

Purpose: Establish the HTTP service infrastructure that Plans 02 and 03 will add report endpoints to. The health endpoint (SRVC-04) enables Docker healthcheck and the service definition (SRVC-05) integrates reasoning-engine into the existing Docker Compose stack.

Output: Working FastAPI app with /health, Dockerfile, docker-compose.yml service, updated requirements.txt, and health endpoint tests.
</objective>

<execution_context>
@/Users/phananhle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phananhle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-fastapi-gateway-and-docker-service/08-CONTEXT.md
@.planning/phases/08-fastapi-gateway-and-docker-service/08-RESEARCH.md

<interfaces>
<!-- Key existing code the executor needs -->

From reasoning/app/pipeline/__init__.py:
```python
async def generate_report(ticker, asset_type, db_engine, neo4j_driver, qdrant_client, db_uri) -> tuple[int, int]:
```

From sidecar/Dockerfile (reference pattern):
```dockerfile
FROM python:3.12-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app/ ./app/
COPY tests/ ./tests/
COPY pytest.ini .
HEALTHCHECK --interval=15s --timeout=5s --retries=3 --start-period=30s CMD curl -sf http://localhost:8000/health || exit 1
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

From docker-compose.yml data-sidecar service (reference pattern for build, depends_on, healthcheck):
```yaml
data-sidecar:
  build:
    context: ./sidecar
    dockerfile: Dockerfile
  mem_limit: 512m
  restart: unless-stopped
  depends_on:
    postgres:
      condition: service_healthy
  environment:
    DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
  healthcheck:
    test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
  networks:
    - ingestion
  profiles: ["ingestion"]
```

Existing reasoning/requirements.txt packages (must preserve — only ADD new lines):
```
llama-index-graph-stores-neo4j>=0.3.0
llama-index-vector-stores-qdrant>=0.3.0
llama-index-embeddings-fastembed>=0.2.0
llama-index-core>=0.11.0
fastembed>=0.3.0
qdrant-client>=1.14.0,<1.17.0
neo4j>=5.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pydantic>=2.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0
langchain-core>=0.3.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI app skeleton with lifespan, health endpoint, and tests</name>
  <files>
    reasoning/app/main.py,
    reasoning/app/dependencies.py,
    reasoning/app/routers/__init__.py,
    reasoning/app/routers/health.py,
    reasoning/tests/api/__init__.py,
    reasoning/tests/api/test_health.py
  </files>
  <behavior>
    - test_health_returns_200: GET /health returns 200 with JSON body containing "status": "ok" and "service": "reasoning-engine"
    - test_health_response_shape: Response includes at minimum "status" and "service" keys
  </behavior>
  <action>
    1. Create `reasoning/app/dependencies.py`:
       - Import `asynccontextmanager` from contextlib, `FastAPI`, `os`
       - Import `create_engine` from sqlalchemy, `GraphDatabase` from neo4j, `QdrantClient` from qdrant_client
       - Define `lifespan(app: FastAPI)` async context manager:
         - Startup: create `app.state.db_engine` from `DATABASE_URL` env var (pool_pre_ping=True, pool_size=5)
         - Create `app.state.neo4j_driver` from `NEO4J_URI` and `NEO4J_PASSWORD` env vars
         - Create `app.state.qdrant_client` from `QDRANT_HOST` and `QDRANT_PORT` env vars (with `QDRANT_API_KEY` if set)
         - Create `app.state.db_uri` storing the raw `DATABASE_URL` string (for AsyncPostgresSaver in run_graph)
         - Initialize `app.state.job_queues: dict[int, asyncio.Queue] = {}` for SSE (Plan 03)
         - Yield
         - Shutdown: `app.state.db_engine.dispose()`, `app.state.neo4j_driver.close()`

    2. Create `reasoning/app/routers/__init__.py` (empty).

    3. Create `reasoning/app/routers/health.py`:
       - FastAPI APIRouter with `tags=["health"]`
       - GET `/health` returning `{"status": "ok", "service": "reasoning-engine"}`
       - Use Pydantic BaseModel `HealthResponse(status: str, service: str)` as response model

    4. Create `reasoning/app/main.py`:
       - Import FastAPI, lifespan from dependencies, health router
       - Configure logging (same pattern as sidecar: basicConfig INFO level)
       - Create `app = FastAPI(title="Stratum Reasoning Engine", lifespan=lifespan)`
       - Register health router: `app.include_router(health.router)`
       - NOTE: Do NOT use deprecated `@app.on_event("startup")` — use lifespan pattern

    5. Create `reasoning/tests/api/__init__.py` (empty).

    6. Create `reasoning/tests/api/test_health.py`:
       - Use FastAPI TestClient with app
       - Override lifespan to avoid real DB connections in tests (use `app.dependency_overrides` or create app without lifespan for unit tests)
       - Test: GET /health returns 200, body has "status"="ok" and "service"="reasoning-engine"

    IMPORTANT: For test isolation, create a test app fixture that does NOT require real database connections. The simplest approach: in the test file, create a minimal test app that imports only the health router (no lifespan), since the health endpoint has no dependencies on app.state.
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum/reasoning && python -m pytest tests/api/test_health.py -x -q</automated>
  </verify>
  <done>GET /health returns 200 with {"status": "ok", "service": "reasoning-engine"}; test passes; lifespan initializes db_engine, neo4j_driver, qdrant_client, db_uri, job_queues on app.state</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfile, requirements.txt updates, and docker-compose.yml service definition</name>
  <files>
    reasoning/Dockerfile,
    reasoning/requirements.txt,
    docker-compose.yml
  </files>
  <action>
    1. Update `reasoning/requirements.txt` — ADD the following lines (preserve all existing lines):
       ```
       # Phase 8 FastAPI service
       fastapi>=0.115.0
       uvicorn>=0.30.0
       sse-starlette>=2.1.0
       httpx>=0.27.0
       # Phase 8 — missing deps for AsyncPostgresSaver (noted in CONTEXT.md deferred)
       langgraph-checkpoint-postgres
       psycopg[binary]
       ```
       Do NOT remove or modify any existing lines.

    2. Create `reasoning/Dockerfile`:
       - Follow sidecar Dockerfile pattern exactly:
       ```dockerfile
       FROM python:3.12-slim
       WORKDIR /app
       RUN apt-get update && apt-get install -y --no-install-recommends curl \
           && rm -rf /var/lib/apt/lists/*
       COPY requirements.txt .
       RUN pip install --no-cache-dir -r requirements.txt
       COPY app/ ./app/
       COPY tests/ ./tests/
       COPY pytest.ini .
       HEALTHCHECK --interval=15s --timeout=5s --retries=3 --start-period=60s \
           CMD curl -sf http://localhost:8000/health || exit 1
       CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
       ```
       NOTE: `start-period=60s` (not 30s like sidecar) — reasoning-engine loads heavier dependencies (LlamaIndex, LangGraph, Neo4j driver).

    3. Add `reasoning-engine` service to `docker-compose.yml`:
       - Place it after the data-sidecar section, under a new comment block
       - EXACT configuration (all from locked decisions):
       ```yaml
       reasoning-engine:
         build:
           context: ./reasoning
           dockerfile: Dockerfile
         mem_limit: 2g
         restart: unless-stopped
         depends_on:
           postgres:
             condition: service_healthy
           neo4j:
             condition: service_healthy
           qdrant:
             condition: service_healthy
         environment:
           DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
           NEO4J_URI: bolt://neo4j:7687
           NEO4J_PASSWORD: ${NEO4J_PASSWORD}
           QDRANT_HOST: qdrant
           QDRANT_PORT: "6333"
           QDRANT_API_KEY: ${QDRANT_API_KEY}
           GEMINI_API_KEY: ${GEMINI_API_KEY}
         healthcheck:
           test: ["CMD", "curl", "-sf", "http://localhost:8000/health"]
           interval: 15s
           timeout: 5s
           retries: 3
           start_period: 60s
         ports:
           - "8001:8000"
         networks:
           - reasoning
         profiles: ["reasoning"]
       ```
       - Use `mem_limit: 2g` (legacy Docker key — locked project decision from Phase 3)
       - Use `curl` for healthcheck (matching Dockerfile HEALTHCHECK pattern)
       - Port 8001:8000 (host:container) per locked decision
       - profiles: ["reasoning"] per roadmap
       - Network: reasoning only
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum && grep -q "reasoning-engine" docker-compose.yml && grep -q "sse-starlette" reasoning/requirements.txt && grep -q "langgraph-checkpoint-postgres" reasoning/requirements.txt && grep -q "psycopg\[binary\]" reasoning/requirements.txt && test -f reasoning/Dockerfile && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Dockerfile exists following sidecar pattern with 60s start_period; requirements.txt has all new dependencies added; docker-compose.yml has reasoning-engine service with mem_limit: 2g, profiles: ["reasoning"], ports: 8001:8000, depends_on postgres/neo4j/qdrant with health conditions, GEMINI_API_KEY in environment</done>
</task>

</tasks>

<verification>
1. `cd reasoning && python -m pytest tests/api/test_health.py -x -q` — health endpoint test passes
2. `grep -c "reasoning-engine" docker-compose.yml` — service appears in compose file
3. `grep "sse-starlette\|langgraph-checkpoint-postgres\|psycopg\[binary\]\|fastapi\|uvicorn" reasoning/requirements.txt` — all 5 new deps present
4. Dockerfile references `start-period=60s` (not 30s)
5. docker-compose.yml reasoning-engine has `mem_limit: 2g`, `profiles: ["reasoning"]`, port `8001:8000`
</verification>

<success_criteria>
- GET /health returns 200 with {"status": "ok", "service": "reasoning-engine"} — test proves it
- reasoning-engine service defined in docker-compose.yml with all locked configuration values
- Dockerfile follows sidecar pattern with 60s start period
- requirements.txt has fastapi, uvicorn, sse-starlette, langgraph-checkpoint-postgres, psycopg[binary] added
- lifespan initializes db_engine, neo4j_driver, qdrant_client, db_uri, job_queues on app.state
</success_criteria>

<output>
After completion, create `.planning/phases/08-fastapi-gateway-and-docker-service/08-01-SUMMARY.md`
</output>
