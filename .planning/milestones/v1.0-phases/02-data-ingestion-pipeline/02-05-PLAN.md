---
phase: 02-data-ingestion-pipeline
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - sidecar/tests/conftest.py
  - sidecar/tests/__init__.py
  - sidecar/tests/test_vnstock_service.py
  - sidecar/tests/test_fred_service.py
  - sidecar/tests/test_gold_service.py
  - sidecar/tests/test_markers_service.py
  - sidecar/tests/test_pipeline_log.py
  - sidecar/tests/test_anomaly.py
  - sidecar/tests/test_timestamp_convention.py
  - sidecar/pytest.ini
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09]

must_haves:
  truths:
    - "pytest test suite validates all 9 DATA requirements"
    - "Zero rows in any Phase 2 table with NULL data_as_of or ingested_at (DATA-07)"
    - "pipeline_run_log has records for every pipeline type with correct status (DATA-08)"
    - "Anomaly detection correctly flags >50% deviations and does not block ingestion (DATA-09)"
    - "End-to-end pipeline produces data in all 7 tables: stock_ohlcv, stock_fundamentals, gold_price, gold_etf_ohlcv, gold_wgc_flows, fred_indicators, structure_markers"
    - "Structure markers have non-NULL MAs for rows with sufficient history (DATA-06)"
    - "FRED data_as_of values span years of observation periods, not clustered on ingestion date (DATA-05)"
  artifacts:
    - path: "sidecar/pytest.ini"
      provides: "pytest configuration"
      contains: "asyncio_mode"
    - path: "sidecar/tests/conftest.py"
      provides: "Shared test fixtures (DB session, mock services)"
      contains: "fixture"
    - path: "sidecar/tests/test_timestamp_convention.py"
      provides: "DATA-07 validation — zero NULL timestamps across all tables"
      contains: "data_as_of.*NOT.*NULL"
  key_links:
    - from: "sidecar/tests/test_vnstock_service.py"
      to: "sidecar/app/services/vnstock_service.py"
      via: "Tests vnstock fetch and upsert logic"
      pattern: "test_ohlcv|test_fundamentals"
    - from: "sidecar/tests/test_timestamp_convention.py"
      to: "all Phase 2 tables"
      via: "SQL query checking NULL counts"
      pattern: "WHERE.*data_as_of.*IS NULL"
---

<objective>
Create the test suite and run end-to-end verification of all Phase 2 requirements. This is the quality gate before Phase 2 is marked complete.

Purpose: Validate that all 9 DATA requirements are met with automated tests, and perform a human-verified end-to-end pipeline check.
Output: Passing pytest suite covering all DATA-01 through DATA-09 requirements, plus human verification of data quality.
</objective>

<execution_context>
@/Users/phananhle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/phananhle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-ingestion-pipeline/02-CONTEXT.md
@.planning/phases/02-data-ingestion-pipeline/02-RESEARCH.md
@.planning/phases/02-data-ingestion-pipeline/02-01-SUMMARY.md
@.planning/phases/02-data-ingestion-pipeline/02-02-SUMMARY.md
@.planning/phases/02-data-ingestion-pipeline/02-03-SUMMARY.md
@.planning/phases/02-data-ingestion-pipeline/02-04-SUMMARY.md

<interfaces>
<!-- All sidecar endpoints and tables from Plans 01-04 -->

Sidecar endpoints:
- POST /ingest/vnstock/ohlcv, /ingest/vnstock/fundamentals
- POST /ingest/gold/fred-price, /ingest/gold/gld-etf, /ingest/gold/wgc-flows
- POST /ingest/fred/indicators
- POST /compute/structure-markers
- GET /health

Phase 2 tables:
- stock_ohlcv (symbol, resolution, OHLCV, data_as_of, ingested_at)
- stock_fundamentals (symbol, period_type, PE/PB/EPS/market_cap/ROE/ROA/revenue_growth/net_margin, data_as_of, ingested_at)
- gold_price (source, price_usd, data_as_of, ingested_at)
- gold_etf_ohlcv (ticker, OHLCV, resolution, data_as_of, ingested_at)
- gold_wgc_flows (period_end, region, fund_name, holdings_tonnes, flows_usd_millions, central_bank_net_tonnes, source_lag_note, data_as_of, ingested_at)
- fred_indicators (series_id, value, frequency, data_as_of, ingested_at)
- structure_markers (symbol, asset_type, resolution, close, ma_10w/20w/50w, drawdown_from_ath, drawdown_from_52w_high, close_pct_rank, pe_pct_rank, data_as_of, ingested_at)
- pipeline_run_log (existing from V1)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pytest test suite for all DATA requirements</name>
  <files>sidecar/pytest.ini, sidecar/tests/__init__.py, sidecar/tests/conftest.py, sidecar/tests/test_vnstock_service.py, sidecar/tests/test_fred_service.py, sidecar/tests/test_gold_service.py, sidecar/tests/test_markers_service.py, sidecar/tests/test_pipeline_log.py, sidecar/tests/test_anomaly.py, sidecar/tests/test_timestamp_convention.py</files>
  <behavior>
    - DATA-01: stock_ohlcv has rows with correct symbol, resolution, OHLCV columns, data_as_of, ingested_at
    - DATA-02: stock_fundamentals has rows with PE, PB, EPS, market_cap, ROE, ROA fields populated
    - DATA-03: gold_price has rows with data_as_of = FRED observation date (spread across years, not clustered on today)
    - DATA-04: gold_wgc_flows has rows OR endpoint returns 501 with documented limitation
    - DATA-05: fred_indicators has rows for GDP, CPIAUCSL, UNRATE, FEDFUNDS with data_as_of spanning observation periods
    - DATA-06: structure_markers has rows with non-NULL ma_10w for rows where data history >= 10 weeks
    - DATA-07: Zero rows in ANY Phase 2 table with NULL data_as_of or NULL ingested_at
    - DATA-08: pipeline_run_log has records for each pipeline with status field
    - DATA-09: anomaly detection returns True for >50% deviation, False for normal counts, False for insufficient history
  </behavior>
  <action>
**sidecar/pytest.ini:**
```ini
[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
```

**sidecar/tests/conftest.py:**
- Create a test database session fixture using the same DATABASE_URL from environment
- Create a fixture that provides the FastAPI TestClient (from httpx or starlette.testclient)
- If running against live database: fixtures read existing data (integration tests)
- If running in isolation: fixtures mock vnstock/yfinance/fredapi responses with known data

**sidecar/tests/test_vnstock_service.py** (DATA-01, DATA-02):
- test_ohlcv_ingest: verify stock_ohlcv has rows with expected columns after POST /ingest/vnstock/ohlcv
- test_ohlcv_has_resolution_column: verify resolution is 'weekly' or 'monthly'
- test_fundamentals_ingest: verify stock_fundamentals has PE, PB, EPS, ROE, ROA populated
- test_vn30_symbols_dynamic: verify get_vn30_symbols() returns a non-empty list

**sidecar/tests/test_fred_service.py** (DATA-03, DATA-05):
- test_gold_price_data_as_of: verify gold_price.data_as_of values are FRED observation dates (check min date is years ago, not today)
- test_fred_data_as_of: verify fred_indicators.data_as_of spans years of observations
- test_fred_all_series_present: verify GDP, CPIAUCSL, UNRATE, FEDFUNDS all have records
- test_fred_frequency_correct: verify frequency column matches expected per series

**sidecar/tests/test_gold_service.py** (DATA-04):
- test_wgc_flow_ingest: verify gold_wgc_flows has records OR endpoint returns 501
- test_wgc_source_lag_note: verify source_lag_note is populated on WGC records

**sidecar/tests/test_markers_service.py** (DATA-06):
- test_ma_non_null: verify ma_10w is non-NULL for rows where symbol has >= 10 weeks of history
- test_both_drawdowns_present: verify drawdown_from_ath and drawdown_from_52w_high columns exist and are computed
- test_close_pct_rank_range: verify close_pct_rank is between 0 and 1
- test_pe_pct_rank_null_for_gold: verify pe_pct_rank is NULL for gold_spot and gold_etf asset_types

**sidecar/tests/test_pipeline_log.py** (DATA-08):
- test_run_log_written: verify pipeline_run_log has records after endpoint calls
- test_run_log_has_required_fields: verify pipeline_name, status, rows_ingested, data_as_of are populated
- test_failure_logged: verify failed pipeline runs create log records with status='failure'

**sidecar/tests/test_anomaly.py** (DATA-09):
- test_row_count_deviation_high: check_row_count_anomaly returns True for >50% deviation
- test_row_count_deviation_normal: returns False for normal counts
- test_row_count_insufficient_history: returns False when < 4 historical runs exist
- test_anomaly_does_not_raise: verify function never raises (always returns bool)

**sidecar/tests/test_timestamp_convention.py** (DATA-07):
- test_no_null_timestamps: For each table (stock_ohlcv, stock_fundamentals, gold_price, gold_etf_ohlcv, gold_wgc_flows, fred_indicators, structure_markers):
  `SELECT COUNT(*) FROM {table} WHERE data_as_of IS NULL OR ingested_at IS NULL` = 0

Add pytest and test dependencies to requirements.txt if not already present: pytest, pytest-asyncio, httpx (for TestClient).
  </action>
  <verify>
    <automated>cd /Users/phananhle/Desktop/phananhle/stratum && docker compose --env-file $(test -f .env.local && echo .env.local || echo .env) exec -T data-sidecar pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>pytest suite passes with all DATA-01 through DATA-09 requirements validated. Zero NULL timestamps in any table. Anomaly detection tested with edge cases. Pipeline logging verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 2 data ingestion pipeline:
- Python FastAPI sidecar with endpoints for vnstock, gold (FRED/yfinance/WGC), FRED macro indicators, and structure markers
- All 7 Phase 2 tables populated via Flyway V2-V5 migrations
- Pipeline run logging on every endpoint
- vnstock row-count anomaly detection
- n8n workflows for weekly and monthly scheduling with Telegram alerts
- pytest suite validating all 9 DATA requirements
  </what-built>
  <how-to-verify>
1. Run the full test suite:
   `docker compose exec data-sidecar pytest tests/ -v`
   — All tests should pass

2. Check data quality in PostgreSQL:
   ```
   docker compose exec postgres psql -U stratum -d stratum -c "
     SELECT 'stock_ohlcv' as tbl, count(*) as rows FROM stock_ohlcv
     UNION ALL SELECT 'stock_fundamentals', count(*) FROM stock_fundamentals
     UNION ALL SELECT 'gold_price', count(*) FROM gold_price
     UNION ALL SELECT 'gold_etf_ohlcv', count(*) FROM gold_etf_ohlcv
     UNION ALL SELECT 'fred_indicators', count(*) FROM fred_indicators
     UNION ALL SELECT 'structure_markers', count(*) FROM structure_markers
     UNION ALL SELECT 'pipeline_run_log', count(*) FROM pipeline_run_log;
   "
   ```
   — All tables should have rows (except possibly gold_wgc_flows if WGC scraping returned 501)

3. Verify DATA-07 (zero NULL timestamps):
   ```
   docker compose exec postgres psql -U stratum -d stratum -c "
     SELECT 'stock_ohlcv' as tbl, count(*) as null_count FROM stock_ohlcv WHERE data_as_of IS NULL OR ingested_at IS NULL
     UNION ALL SELECT 'stock_fundamentals', count(*) FROM stock_fundamentals WHERE data_as_of IS NULL OR ingested_at IS NULL
     UNION ALL SELECT 'gold_price', count(*) FROM gold_price WHERE data_as_of IS NULL OR ingested_at IS NULL
     UNION ALL SELECT 'fred_indicators', count(*) FROM fred_indicators WHERE data_as_of IS NULL OR ingested_at IS NULL
     UNION ALL SELECT 'structure_markers', count(*) FROM structure_markers WHERE data_as_of IS NULL OR ingested_at IS NULL;
   "
   ```
   — All null_count values should be 0

4. Verify FRED data_as_of spread:
   `docker compose exec postgres psql -U stratum -d stratum -c "SELECT series_id, MIN(data_as_of), MAX(data_as_of), COUNT(*) FROM fred_indicators GROUP BY series_id;"`
   — data_as_of should span years (not clustered on today)

5. Check pipeline_run_log:
   `docker compose exec postgres psql -U stratum -d stratum -c "SELECT pipeline_name, status, count(*) FROM pipeline_run_log GROUP BY pipeline_name, status;"`
   — Should have records for each pipeline with 'success' status

6. Open n8n UI at http://localhost:5678 and verify imported workflows look correct
  </how-to-verify>
  <resume-signal>Type "phase 2 approved" if all checks pass. Describe any issues otherwise.</resume-signal>
</task>

</tasks>

<verification>
1. `pytest tests/ -v` — all tests pass
2. All 7 Phase 2 tables have rows with non-NULL data_as_of and ingested_at
3. FRED data_as_of values span observation periods across years
4. pipeline_run_log has records for every pipeline type
5. Anomaly detection tests pass (true for >50% deviation, false for normal, false for insufficient history)
6. Structure markers have correct MAs, drawdowns, and percentile ranks
7. n8n workflows import and display correctly
</verification>

<success_criteria>
- All 9 DATA requirements (DATA-01 through DATA-09) validated by automated tests
- Zero NULL timestamps in any Phase 2 table (DATA-07)
- End-to-end pipeline produces data in all tables
- Human verification confirms data quality and n8n workflow correctness
- Phase 2 success criteria from ROADMAP.md all met
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-ingestion-pipeline/02-05-SUMMARY.md`
</output>
